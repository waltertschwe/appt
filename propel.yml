propel:
    database:
        connections:
            default:
                adapter: mysql
                dsn: mysql:host=localhost;port=3306;dbname=appointments
                user: root
                password: artecs
                settings:
                    charset: utf8
